# @specify-sh/core

Shared implementation behind Specify's SDKs. It carries no stable API of its own and is not meant to be installed directly.

Install `@specify-sh/publisher-sdk` instead.

**[Get started →](https://docs.specify.sh/publishing/get-started)**

MIT © The Internet Community Company
