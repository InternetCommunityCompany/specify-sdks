---
"@specify-sh/core": minor
"@specify-sh/publisher-sdk": patch
---

Publish the shared SDK core as a public package with JavaScript and TypeScript declarations. The publisher SDK now consumes core as a normal runtime dependency instead of copying its implementation into the publisher package.
